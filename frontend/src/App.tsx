// src/App.test.tsx
import { describe, it, expect } from 'vitest'

describe('App', () => {
  it('placeholder — Phase 0 frontend scaffold', () => {
    expect(true).toBe(true)
  })
})

function App() {
  return (
    <div>
      <h1>EWC Compute</h1>
    </div>
  )
}

export default App

